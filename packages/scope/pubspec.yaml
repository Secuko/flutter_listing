name: scope
description: app scope helper
version: 0.0.1

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.13.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: 8.1.5
  pure: 0.2.0

dev_dependencies:
  app_linter:
    path: ../app_linter
